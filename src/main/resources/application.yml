spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  jackson:
    time-zone: Brazil/East
    locale: pt-BR
externalUrl: https://iws-recruiting-bands.herokuapp.com/api/full
